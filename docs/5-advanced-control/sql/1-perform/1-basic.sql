-- ============================================================
-- PERFORM の基本
-- ============================================================
-- PERFORM は SELECT の結果を破棄して実行する PL/pgSQL 固有の構文
-- 関数の呼び出しや副作用のある処理で使う

DO $$
BEGIN
    PERFORM pg_sleep(0.1);
    RAISE NOTICE '0.1秒待機しました';
END;
$$;
